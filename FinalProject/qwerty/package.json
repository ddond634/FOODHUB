{
  "name": "foodhub-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf .vercel_build_output && mkdir -p .vercel_build_output/static && cp -r frontend/* .vercel_build_output/static/"
  }
}
